import '@mantine/core/styles.css';
import { Button, MantineProvider } from '@mantine/core';

function App() {
  return (
    <MantineProvider>
      <Button>Test</Button>
    </MantineProvider>
  );
}

export default App;
